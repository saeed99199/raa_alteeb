[phases.build]
cmds = [
  "cd backend && composer install --no-dev --optimize-autoloader",
  "cd backend && chmod -R 775 storage bootstrap/cache"
]

[start]
cmd = "cd backend && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
